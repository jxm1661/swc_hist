# Randomizing data, generating summary statistics and histogram

import numpy as np

mu =  80

sigma = 10

x = np.random.normal(mu, sigma, 100)6

print("Random Normal Array Mean Centered ", X[:10])


print("mean", mp.mean(x))
